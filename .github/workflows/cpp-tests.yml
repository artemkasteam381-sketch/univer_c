name: C++ CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y g++ cmake make libgtest-dev
        
    - name: Setup GoogleTest
      run: |
        cd /usr/src/gtest
        sudo cmake CMakeLists.txt
        sudo make
        sudo cp lib/*.a /usr/lib
        
    - name: List files (debug)
      run: ls -la
      
    - name: Compile tests
      run: |
        g++ -std=c++17 -I. TestHeap.cpp -o heap_test -lgtest -lgtest_main -lpthread
        
    - name: Run tests
      run: |
        ./heap_test
        
    - name: Compile example
      run: |
        g++ -std=c++17 -I. example.cpp -o heap_example
        
    - name: Run example
      run: |
        ./heap_example
